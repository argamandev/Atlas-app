# cross-cutting — APPEND-ONLY alerts log (current era: from 2026-08-09)

<!-- Purpose: "someone changed the ground under you". APPEND before applying a migration,
     changing shared types/design tokens, or changing lib/api / lib/db shapes. Read it before
     big moves. Appends go via >> or fs.appendFileSync — never rewrite, never Edit.
     FOUNDER DECISIONS now live in agent-memory/DECISIONS.md (permanent, never compacted).
     ERAS BEFORE 2026-08-09, VERBATIM AND COMPLETE:
       docs/archive/cross-cutting-2026-07-03--2026-08-10.md
     Compacted 2026-08-10 by fleet-lint with founder approval, after a verified snapshot.
     Entries kept here: everything from the current era, plus older items still OPEN. -->

[2026-08-09 Lane M] MIGRATION 021 ABOUT TO BE APPLIED to the shared DB — supabase/migrations/20260809_021_scheduled_calls_maya.sql on branch feat/maya-calendar. ADDITIVE ONLY, table public.scheduled_calls: five new columns (kind text default 'call', time_known boolean default true, maya_year int, maya_period_type_id int, maya_report_type_id int), two CHECK constraints (kind vocabulary; and the maya_* key parts must all be present when source='maya'), and the table's FIRST unique constraint (company_id, maya_year, maya_period_type_id, maya_report_type_id). No index is added. Existing 4 rows are all source='mock' and satisfy both CHECKs. RLS unchanged, no user_id — scheduled_calls stays shared corpus. WHY: the schedule feed ships no row id, so without that key a re-run duplicates every synced row. Reviewed as a FILE first per rules/db.md; the cold reviewer returned CHANGES with 3 blockers and this is the second draft.
[2026-08-09 Lane M] FINDING + CORRECTION — the MAYA API has company sector/website/logo after all; I said it did not, in four documents. The founder refused the claim ("I can see the sector and the logo on the MAYA website") and he was right. What is now verified: (1) the TASE Data Hub catalogue holds 41 PRODUCTS; Atlas is registered for ONE VERSION OF ONE of them — `Market Announcements feed - MAYA 2.0.0`, app "Atlas - second application", approved 2026-08-05. (2) That product has TWO versions and they are different APIs, not a version bump: 2.0.0 = `/api/v2/market-announcements/...` (the 5 endpoints in docs/MAYA-API.md, all we can call); 1.0.0 = `/v1/maya-reports-online/...`, TEN endpoints, including `GET /v1/maya-reports-online/company-details` whose response carries issuerId, issuerName, sector, address, website, phone, email — and whose `issuerId` parameter is `required: false`, i.e. ONE call returns the whole company directory that maya-refresh-issuers.ts spends ~230 (or ~2,600 with --sweep) requests building by hand. FIELD LIST IS FROM THE OPENAPI SPEC, NOT A LIVE 200 — we cannot call it until registered, and registering is an ACCOUNT ACTION that belongs to the founder (portal -> Catalog -> Market Announcements feed - MAYA -> version 1.0.0 -> Register). Nobody should record those fields as "verified" until a 200 exists. (3) LOGOS ARE ALREADY OURS, no subscription and no key: `mayafiles.tase.co.il/logos/he-IL/{issuerId padded to 6}.jpg`, the same public host as the filing attachments. MEASURED across all 233 companies carrying a tase_issuer_id: 220 real distinct logos, 13 sharing one placeholder PNG, 0 non-200. Two ingest traps, both measured: the content-type LIES (issuer 2356 is served `image/jpeg` and is a PNG) so sniff magic bytes; and there are TWO placeholders (2,325-byte JPEG `000000.jpg`, 2,037-byte PNG) so hash-compare against both or 13 companies get a grey square shown as their identity. THE METHOD LESSON, which is why this is an alert and not a note: I probed guessed paths, got an F5 WAF "Request Rejected" HTML page, and read that failure as ABSENCE. A 403 is only evidence if you know WHO SENT IT — Kong's `{"message":"You cannot consume this service"}` means not-subscribed and proves the route exists; the WAF's HTML means the gateway never saw the request and proves nothing at all. docs/MAYA-API.md already carried "a base URL is configuration, read from the system that issues it, never deduced"; a PATH LIST is configuration too, and the portal spec had it all along. Everything above is filed in docs/MAYA-API.md (two new sections). Corrections left visible in docs/evidence/feat-maya-calendar/ and state-multiview.md rather than quietly edited. NOT ACTED ON: no subscription requested, no code written — feat/maya-calendar stays as reviewed at 43936cc plus these doc corrections.
[2026-08-09 Lane M] MAYA 1.0.0 IS APPROVED AND VERIFIED — the founder registered it the same day the gap was found, and `GET /v1/maya-reports-online/company-details` now answers 200. ONE request with no parameters returns 1,630 COMPANIES (1.28 MB): issuerId, issuerName, sector, about, website, address, zip, phone, fax, email, incorporation, securityIncludedIndices. Coverage measured: sector 1630/1630 across 67 distinct sectors; website 1005/1630 (62%); `about` is a real Hebrew business description. THREE CONSEQUENCES FOR WHOEVER TAKES THE COMPANY-PAGES SLICE: (1) `securityIncludedIndices` carries index membership WITH WEIGHTS, so the index chips deleted from CompanyView on feat/maya-calendar as fabricated have a real source and can return as facts. (2) 1,630 companies in one request is a strict superset of what `maya-refresh-issuers.ts --sweep` spends ~2,600 requests and ~9 minutes to find — DO NOT RUN THE SWEEP, retire it. (3) `sector` is SPACE-PADDED and hierarchical ("ריאלי-מסחר ושרותים-שרותים          ") — trim, then split on `-` into super/sector/sub, the same three levels the Securities-Basic product exposes as separate fields. ALSO PROBED, all 200: `events/company` returns the FULL 227-type company disclosure vocabulary — which makes the line in docs/MAYA-API.md saying "there is no endpoint for the full disclosure-event table" false, and it is now corrected in place; `events/tase` returns 44 notice types; `latest-companies-disclosures` returns the 30 most recent filings live, and the first sample was itself a schedule CORRECTION ("שיחת ועידה הוקדמה לשעה 11:00"), i.e. that endpoint is a candidate for keeping the calendar honest as calls move. `corporate-actions/assembly/by-dates` 500s without date parameters. FOR THE WEBINAR SLICE SPECIFICALLY: event type 271 "אירועי משקיעים" exists as a STRUCTURED code, so webinars can be ENUMERATED rather than pattern-matched out of titles — but whether a webinar's DATE is structured or sits in Hebrew prose is NOT yet established, and that question decides whether the slice is plumbing or extraction. Answer it before planning the slice. All of the above is filed in docs/MAYA-API.md.
[2026-08-09 15:40] MERGE supervisor — fix/calendar-empty-state (carrying Lane M's feat/maya-calendar + feat/company-profiles) → main (a94f33d), pushed 55ffdf0..a94f33d. Battery on the real merge result, byte-identical to the reviewed tip: 610/610 tests · tsc exit 0 · build green · Middleware 81.8 kB. FOUR review rounds, three of which returned CHANGES. SHARED-SURFACE CHANGES EVERY LANE SHOULD KNOW: (1) MIGRATION 021 IS LIVE on the shared production DB — scheduled_calls gains kind, time_known, maya_year, maya_period_type_id, maya_report_type_id, a kind CHECK, a whole-key CHECK and the UNIQUE (company_id, maya_year, maya_period_type_id, maya_report_type_id) natural key; additive only, reviewed as a FILE before apply per rules/db.md. Hand-added calls MUST leave the maya_* columns NULL or they collide. (2) THE DATA UNDER YOU CHANGED: companies 5 -> 234, scheduled_calls 4 -> 895 (891 MAYA + the 4 pre-existing mock), 470 rows with no published time. Any fixture, screenshot or count you took before today is stale. (3) lib/company/overview-stub.ts IS GONE and so are the hardcoded "Q2 2026" call sites — if your branch imports the stub it will not compile, and the answer is the real feed, never a new stub. (4) NEW SHARED MODULES: lib/company/logo.ts (logo resolution, pure + tested — it was extracted from db/companies.ts because sitting behind server-only made it untestable, which is how a name-substring guess put one issuer's mark on another for months; NEVER resolve a logo from a name) and calendarEmptyState in lib/calendar/event-meta.ts. (5) DICTIONARY: calendar.allTypesHidden was renamed monthHiddenByFilter and three keys added (monthHiddenByFilterMine, noEventsThisMonthMine, plus company.about now rendered) — both locales, parity is compiler-enforced. (6) ARCHITECTURE.md's test enumeration was regenerated and now carries the COMMAND that produces it; the previous sentence claimed a script emitted it and no such script has ever existed, which is how it rotted to 38 names against 65 registered. Regenerate, do not hand-edit.
[2026-08-09 15:40] LESSON (graduated to .claude/rules/app.md) — A CHOKE POINT IS ONLY AS HONEST AS ITS INPUTS. Addendum to the choke-point rule filed on feat/workspace-tables. The calendar empty state took FOUR rounds: a JSX guard that could never be false; then the decision correctly moved to one testable function but fed a PROXY (whole-feed event kinds) instead of the fact it was deciding about (this month's contents before filtering); then the test written beside it asserted the surviving lie, so the battery DEFENDED the defect; then the supervisor's own fix said the same untrue sentence in the other view mode. Putting the invariant at a single choke point is necessary and NOT sufficient — given a stand-in for the fact, it decides confidently and wrongly, and the adjacent test makes that permanent. Ask what the question is ABOUT, and pass that. Second half of the lesson: round 4 was invisible to 610 passing tests, a clean tsc and a green build, and was caught by a cold reader; it lived in a state nobody had ever rendered. NINE states were then driven in a browser in both locales, which is what finally closed it.
[2026-08-09 15:40] ALERT — TWO ORPHAN ROWS IN followed_calls, filed not fixed. Both rows in public.followed_calls carry user_id 00000000-0000-0000-0000-000000000000 (the deleted DEMO_USER_ID shared identity), created 2026-06-13/14, with no matching auth.users row. They persist because followed_calls is one of the five tables rules/db.md records as having user_id NOT NULL and NO foreign key to auth.users. Harmless today — they belong to nobody, so no user sees them — but it is what a missing FK looks like in production, and it is the residue the 2026-08-03 DEMO_USER_ID removal could not reach. Found while verifying the calendar's My-calendar states; the supervisor's own follow/unfollow round-trips were verified to leave nothing behind.
[2026-08-09 16:45] MERGE supervisor — fix/israel-time → main (796fdbd), pushed. PRODUCTION HOTFIX: the live site listed every investor call THREE HOURS EARLY on Home. FLEET-WIDE RULE, and it is now the law in .claude/rules/app.md: NEVER format an instant without a timeZone. src/lib/i18n/format.ts pins Asia/Jerusalem (ISRAEL_TZ) and exports israelDayKey / israelMonthParts; use them for any day bucketing. WHY IT WAS INVISIBLE: Home is a SERVER Component, so it formatted on Railway in UTC and passed the client a finished STRING the browser never re-formats, while the calendar (a client component) formatted in the browser and was correct — two surfaces, one event, three hours apart. It could not be seen on any machine in Israel, which is every machine this was developed on. Battery now runs green in BOTH timezones (618/618 under TZ=UTC and Asia/Jerusalem); if you add a formatter, test it under TZ=UTC or you are only testing your laptop. STILL OPEN, next branch not someday: the calendar today-pill and CompanyOverview isFuture still read the viewer local midnight, and lib/db/calls.ts scope:upcoming floors at UTC midnight.
[2026-08-09 18:10 Lane M] DEFERRED SLICE FILED, NOT BUILT — the DOCUMENTS CATALOG ("documents over the years"). Founder asked for it, then deferred it the same day to its own session so the smart-chat-layer brainstorm can go first. Full measured brief: docs/product/2026-08-09-documents-catalog-findings.md. THREE CORRECTIONS THE SUPERVISOR SHOULD CARRY, because each reverses something previously stated to the founder: (1) IT IS NOT BLOCKED ON A MIGRATION. company_documents.quarter already holds "FY 2024"/"Q1 2025" and CompanyView already has yearOf() — year/quarter bucketing needs no new column. The publication-date column is for ordering WITHIN a period and is not a prerequisite. (2) NO BULK SYNC AND NO CATALOG TABLE — the founder proposed on-demand fetching "like workspace does with the PDFs" and he was right; it is literally the same code. listDisclosures() lists an issuer's whole catalog live from MAYA, ingestFiling() stores ONE filing when a user opens it. Measured ~1.6s to list 4 years for one company. (3) INVESTOR PRESENTATIONS ALREADY WORK — EVENT_PRESENTATION=270 (מצגת) was already whitelisted and already mapped to docType 'slides'. LIVE PROBE, 15 companies sampled evenly across the 233 with a tase_issuer_id, 2023-2026: 272 reports, 131 presentations, 14 of 15 companies carry at least one deck, 76 periods carry BOTH a report and a deck. Presentations are PDFs, so the Slides pane is the existing PdfViewer pointed at another file. TWO CONSTRAINTS FOR WHOEVER PLANS IT: (a) THE RATE LIMIT IS THE BINDING ONE — 4-5 requests per company page view out of the 10-per-2-seconds budget shared by our whole key, so this IS the recorded trigger for the priority queue deferred 2026-08-06; cache the listing inside the slice, not after it. (b) THE UPSERT KEY COLLIDES FOR DECKS, and the remedy is NOT the lane's to choose: company_documents is unique on (company_id, quarter, doc_type), and periodFor() gives a standalone presentation the BARE YEAR as its period, so every deck a company files in one year maps to one row and the last one written wins — silent wrong content on re-open. Widening that key means removing a UNIQUE constraint on a table in the shared production DB, which rules/db.md routes to supervisor + founder before anything is written. Listing is unaffected; only what we store after a click. ALSO FOUND WHILE MEASURING, and it is the honest blocker on "just connect the viewer": SlidesPane calls slideStubs() UNCONDITIONALLY (FacetPanes.tsx:103) — four invented Hebrew slides about אפגלו and Qatari/Saudi sovereign wealth funds, rendered for every call of every company. It is the sibling of the filed reportStub FINDING 2026-07-17 and both must go when this slice lands. Current stored floor for context: company_documents holds 12 rows across 3 of 234 companies, all doc_type='report', ZERO presentations; transcripts 60 rows with only 2 companies linked. Estimate 2-3 days. Founder decision taken: the viewer opens in MULTI, and it is the SAME LiveTranscriptView, not a lookalike. One thing NOT measured and worth measuring before designing the key fix: how often one company files 2+ decks mapping to the same period label.
[2026-08-09 18:05] ⚠ SUPERVISOR FINDING — ONE PIECE OF THE DEFERRED FEATURE MUST NOT TRAVEL WITH
IT: THE PUBLICATION-DATE COLUMN. Verified against the LIVE DB just now, not recalled:
`information_schema.columns` for public.company_documents returns
  id · company_id · quarter(text) · doc_type · title · source · storage_path · page_count · lang
  · created_at · updated_at · maya_report_id
— i.e. there is NO publication date. `created_at` is WHEN ATLAS INGESTED THE ROW, which is a
different fact about the world: the 12 live rows were ingested 2026-07-16..2026-08-09 and say
nothing about when the issuer actually published them.
WHY THIS BLOCKS THE PRIORITISED WORK AND NOT JUST THE DEFERRED ONE: Q1 of the agent spec is "what
does the agent do when it does not know", and every honest answer of that shape — "I have this
issuer through Q2 2026", "nothing has been filed since March" — IS A STATEMENT ABOUT PUBLICATION
DATES. An agent reasoning about freshness from `created_at` would call a 2024 filing ingested
yesterday current. That is the silent-degradation class, aimed at a fund.
COST OF DOING IT NOW vs LATER, measured: **12 rows, 3 companies, 11 of 12 carrying maya_report_id**
(so MAYA can supply the real date for essentially all of them). This is the cheapest this migration
will ever be, and this database is shared with production Timlul under an additive-only law —
every filing ingested from here on makes the backfill bigger.
⇒ RECOMMENDATION: the publication-date column + `ingestFiling`'s upsert key are decided by the
AGENT PLAN spec (Q2, the corpus question) and shipped with the retrieval foundation, NOT parked
with the catalog UI. Deferring the screen is right; deferring the column is the founder's own
standing rule inverted ("defer auth wiring, never defer schema").
[2026-08-09 18:40] ⚠⚠ SUPERVISOR FINDING — THE CORPUS IS NEARLY EMPTY, AND THIS OUTRANKS EVERY
OTHER ITEM IN THE AGENT PLAN. Measured against the LIVE DB just now, prompted by a founder
question ("we have the database of all of the companies right?"). Atlas has the companies and
their CALENDAR. It does not have their CONTENT.
    companies                                     234
    scheduled_calls                               895   (467 kind=report · 428 kind=call,
                                                         2026-02-08 … 2027-03-25)
    transcripts, completed                         56
      └ of those, WITH a company_id                 5   ← across **2 distinct companies**
      └ of those, with NO company_id AT ALL         51
    company_documents                              12   ← across 3 distinct companies
⇒ **A COMPANY-SCOPED AGENT CAN FIND READABLE CONTENT FOR 2 OF 234 COMPANIES.** And 51 completed
transcripts — the majority of everything Atlas has ever produced — are ORPHANED: they carry no
company_id, so no company-scoped query reaches them. They are invisible to a per-company agent
even though the text exists.
WHY THIS WAS NOT VISIBLE BEFORE: `getChatContext()`'s fallback chain ends at "the newest completed
transcript ANYWHERE", so chat has always looked like it works. It has been answering from whatever
it could find. The 234-company figure that landed 2026-08-09 is COMPANIES AND EVENT METADATA from
MAYA — name, ticker, sector, sub-sector, description, and when things are scheduled. Nobody
claimed otherwise; the two numbers simply live in different sentences and had not been put side
by side.
⇒ CONSEQUENCE FOR THE AGENT PLAN, and it REORDERS Q2/Q3: the first question is not "how does an
agent SEARCH the corpus" but **"what IS the corpus, and how does content GET IN"** — ingestion,
attribution (the orphaned 51), and coverage per company. A retrieval architecture chosen against
a corpus of 5 attributed transcripts is chosen against nothing. Prove the corpus question before
spending a decision on pgvector.
⇒ AND IT REFRAMES THE DEFERRED FILINGS CATALOG. Fetch-on-demand is a RENDERING path for a human
reader. Whether that fetch also STORES AND ATTRIBUTES what it pulled is the difference between a
document viewer and a corpus — the same mechanism, one design decision apart. The catalog SCREEN
stays deferred (founder, 18:05, and that is right). The INGESTION QUESTION underneath it belongs
to the agent spec.
[2026-08-09 18:40] ↩ SUPERVISOR CORRECTION to the 18:05 finding above, filed because this repo's
rule is that a count comes from a command and so does a claim. That entry said the missing
`company_documents` publication-date column is what the agent's honesty rests on ("I have this
issuer through Q2 2026"). THAT OVERSTATED IT. `scheduled_calls` already holds **467 report events
with real dates** plus maya_year / maya_period_type_id / maya_report_type_id (migration 021), so
"has this issuer filed anything since March" is largely answerable TODAY without that column. The
column is still worth taking while the table is 12 rows — a STORED document's own publication date
is not derivable from an event feed — but it is an input, not the linchpin, and the headline
finding is the entry above it. The recommendation stands; the reasoning that justified it does not.
[2026-08-09 18:47] MERGE supervisor — fix/israel-time-residue → main (0b593c0; doc truth 9fbd475; pushed 7a10b85..9fbd475). Closes the three timezone gaps the hotfix left open, plus a bonus fourth (CalendarView initialMonth opened on the VIEWER month). Battery 625/625 under FIVE genuinely applied zones · tsc 0 · build green · Middleware 81.8 kB. FLEET-WIDE, READ THIS BEFORE YOUR NEXT TZ CHECK: in Git Bash a TZ= prefix containing a slash is SILENTLY DROPPED (TZ=Asia/Tokyo → resolves Asia/Jerusalem); only slash-free names survive. It disarmed the lane evidence doc AND the supervisor merge report in the same hour. Verify the zone from inside the run (Intl.DateTimeFormat().resolvedOptions().timeZone) or set it from PowerShell. Graduated to .claude/rules/app.md. NEW SHARED CODE: israelDayStart(dayKey) in lib/i18n/format.ts (inverse of israelDayKey, for the one caller that needs a real instant for SQL — probed exact on 730/730 days of 2026-27) and isFutureEvent() in lib/calendar/event-meta.ts (use it, never an inline startOfToday). STILL OPEN, user-visible: src/lib/transcripts.ts:33 renders a transcript created 00:00-03:00 Israel a day early.
[2026-08-09 20:10 Lane M] REFERENCE — HOW THE DOCUMENT CORPUS IS STRUCTURED TODAY, measured against the LIVE DB, filed here because the agent/chat brainstorm tomorrow will search for exactly this and it must not live only in a chat. Counts: companies 234 (233 with tase_issuer_id) · scheduled_calls 895 · company_documents 12 across 3 companies (ALL doc_type='report', ZERO presentations) · document_pages 1,972 · transcripts 60, 56 completed, only 5 carrying a company_id. All five tables have RLS enabled with policies (shared-corpus reads per docs/DATA-MODEL.md). WHERE A FETCHED PDF LANDS, three places at once: (1) BYTES → private storage bucket `company-documents` at `<company-uuid>/maya/<mayaReportId>.pdf` — keyed by the FILING, so bytes never collide; (2) ONE ROW → company_documents (company_id, quarter e.g. "FY 2025", doc_type report|slides, title, lang, source, storage_path, page_count, maya_report_id); (3) TEXT → document_pages, ONE ROW PER PAGE (document_id, page_no, text). So company → period → type is the structure, as a flat table with UNIQUE (company_id, quarter, doc_type), not nested folders. TWO INDEXES THAT MATTER AND WERE NOT PREVIOUSLY WRITTEN DOWN: `company_documents_maya_report_uniq` — UNIQUE on maya_report_id WHERE NOT NULL, so ONE MAYA filing can exist as at most one row in the whole table, enforced by the database; and `document_pages_document_idx`. THE PART THAT IS ALREADY RIGHT FOR RETRIEVAL: text is stored page-addressable AND company-attributed, so a citation can name a page and an embedding table hangs off document_pages additively. THE THREE GAPS, ranked for the agent spec: (1) 51 of 56 completed transcripts carry NO company_id — the majority of Atlas's own transcript text is unreachable by any company-scoped query; measured, they are DEV RESIDUE from 2026-05-12..06-10 (titles like "אמפא Q1 2026 — Claude V2 + thinking (+ report)" — the same call re-transcribed to compare prompts — plus Afcon/Qualitau webinars stored several times), 50 of 51 from YouTube and 25 carrying a user_id, so deletion is destructive SQL on a DB shared with production Timlul and goes through founder + supervisor. (2) NO publication date on a stored document — created_at is Atlas's INGEST time, so an agent asked "what is the newest filing you hold" would call a 2024 filing ingested yesterday current. (3) ONE ROW PER (company, period, type) — the corpus keeps the most recently opened version, so it cannot hold the Hebrew and English editions of one report side by side. NONE of the three blocks the catalog being built today, and today's slice adds NO schema and NO new write path (it calls the existing ingestFiling → ingestDocument), so tomorrow can change the row shape as a one-module change behind lib/documents.
[2026-08-09 20:10 Lane M] LESSON — A `TZ=` PREFIX IS SILENTLY DROPPED IN GIT BASH WHEN ITS VALUE CONTAINS A SLASH. MSYS path conversion eats it, so `TZ=America/New_York npm test` runs in Asia/Jerusalem and prints a reassuring green. Three of the four timezone runs in fix/israel-time-residue's evidence never happened, and the supervisor's own merge check repeated it an hour later — reported as four-zone coverage when it was two. Caught by a cold reviewer checking the ENVIRONMENT rather than the command. ⇒ Run TZ variants from PowerShell (`$env:TZ='UTC'; npm test`) and PRINT the resolved zone inside the run (`Intl.DateTimeFormat().resolvedOptions().timeZone`). A check that cannot fail is worse than no check: it is the thing that was supposed to notice.
[2026-08-09 20:40 Lane M] SHARED TYPE CHANGE (additive) on feat/documents-catalog — Company in src/lib/api/types.ts gains taseIssuerId: string | null, and mapCompany in src/lib/db/companies.ts populates it from the tase_issuer_id column that COLS ALREADY selects. Filed BEFORE the edit per parallel-work.md. Why: the documents catalog lists a company filings from MAYA by issuer id, and three call sites need it (the filings route, the open-document route, the period page). The column was already being read and then dropped on the floor by the mapper, so nothing new is queried. CompanyLite is a Pick and is unchanged, so no component rendering a company card is affected. Additive to a TypeScript interface; no database change.
[2026-08-09 22:55] MERGE supervisor — feat/documents-catalog → main (47bf674; merge-time fixes 227edd5; doc truth de76f61; pushed 9fbd475..de76f61). Lane M, 12 commits, reviewed tip 1d4b9c7. Verdict CHANGES with NO blockers; the warnings worth fixing were fixed at merge and the rest are filed in the ready queue. Battery on the real merge result: 652/652 under Asia/Jerusalem, UTC and America/New_York (zones set from PowerShell, resolved zone printed INSIDE each run) · tsc 0 · build green · Middleware 81.8 kB unchanged.
WHAT SHIPPED: a company page lists the fiscal years it filed in, a year opens to Q1/Q2/Q3/Annual, a period opens report + presentation + transcript-if-any into the SAME LiveTranscriptView a live call uses. NO SCHEMA — no new table, no new column, no migration, verified by command (`git diff --name-only main...HEAD -- supabase/` empty, no DDL in any added line). The catalog lists LIVE from MAYA and stores one PDF on open through the existing ingestFiling path, so the screen IS Atlas's ingestion path: every document a user opens becomes corpus with a real company_id.
FLEET-WIDE CONSEQUENCES, read before your next branch:
(1) **slideStubs() and reportStub() are DELETED and src/lib/live/call-stubs.ts no longer exists.** If your branch imports them it will not compile, and the answer is the real document or an honest empty state — never a new stub. They had been rendering four invented Hebrew slides about אפגלו and Gulf sovereign wealth funds for EVERY call of EVERY company on the live host. The FacetPanes now end in exactly three states: loading · error · noDocument.
(2) **isDocumentEvent now REFUSES filings carrying MAYA event 113** (a notice that a report is coming is not the report). This changes WHAT WORKSPACE OFFERS too, not only the new screen — deliberate, fixed at the source per the choke-point rule. The reviewer proved the fix by mutation against main's events.ts and established isDocumentEvent has exactly ONE production consumer (toRemoteSources), and that isScheduleEvent has ZERO, so the calendar and the live-call path cannot be affected.
(3) **A SHARED TYPE CHANGED (additive): Company gains taseIssuerId** in src/lib/api/types.ts, populated by mapCompany from a column COLS already selected and dropped on the floor. CompanyLite is a Pick and is unchanged.
(4) **TranscriptSidePanel's prop changed shape: `sub: string` → `subParts: string[]`.** If you render that component, pass the runs SEPARATELY — they must not be pre-joined, because a bidi-mixed line cannot be wrapped in <bdi> per run after concatenation. The compiler will catch you.
(5) **MAYA RATE LIMIT — THE PROFILE CHANGED IN KIND, and the priority queue is now genuinely due.** Before today a MAYA listing was spent only on an explicit Workspace intake: deliberate and occasional. Now every view of a company's Documents tab costs ~2 requests and every year click ~2 more, against the 10-per-2-seconds budget shared by the WHOLE product and every user. A 5-minute per-company-year cache (lib/maya/catalogCache.ts) absorbs re-opens and is not a queue. The branch evidence originally called this "unchanged in kind"; that was corrected at merge.
(6) **NEW SHARED MODULES:** lib/company/documentCatalog.ts (pure: filings → years → periods → which filing fills a slot; the Israeli filing calendar has no Q4), lib/documents/openFiling.ts (the needsIngest identity guard — what makes "no schema" safe; NOT atomic, see ARCHITECTURE known gap 7), lib/maya/catalogCache.ts.
(7) **src/lib/transcripts.ts no longer takes the UTC day off created_at** — it uses israelDayKey, so the last user-visible survivor of this morning's timezone class is closed.
⚠ (8) **THE <bdi> RULE IS AT SEVEN OCCURRENCES AND THREE OF THEM WERE FOUND TODAY, so read this before you write dir="ltr".** A formatted Hebrew date ("31 במרץ 2024") is a MIXED run, not a numeral: dir="ltr" on it renders "במרץ 2024 31". The lane fixed the construct in DocumentsTab and wrote a careful comment, while the identical construct sat on the header its own feature feeds a date into; grepping the CONSTRUCT (`git grep -n 'dir="ltr"' -- src`) rather than the component then found two MORE, both pre-existing and user-visible (LiveBroadcastView's live-call header, CompanyOverview's latest-call date). GRADUATED to rules/app.md with the addendum: **grep the construct, not the component — and PROVE the fix renders differently by restoring the old dir on the live element and re-measuring, because a <bdi> that changes nothing looks exactly like a <bdi> that fixes everything.** dir="ltr" remains CORRECT for bare numerals and tickers (formatTime's "10:00" sites were checked and deliberately left).
⚠ (9) **LINE ENDINGS: both i18n dictionaries were committed CRLF into an LF repo with a bare CR welding two keys onto one line** — 2774 lines of diff for 26 real key changes, and it would have collided with any lane touching a dictionary. Normalized at merge. The repo has NO .gitattributes while core.autocrlf=true, so nothing structurally prevents recurrence; `* text=auto eol=lf` is proposed and needs a founder/fleet decision rather than riding a feature merge. Until then: after any scripted edit, check `git diff --stat` against `git diff -w --stat` — if they disagree wildly, you rewrote line endings.
[2026-08-09 23:40] FINDING supervisor · workspace intake · **A CORRECTION TO THE COMPANY NAME IS INVISIBLE TO COMPANY RESOLUTION, SO MAYA IS NEVER ASKED.** Founder report: asked for a yearly report of בז"א, was refused, restated as "בית זיקוק אשדוד" — still refused, "I don't have those files". Root-caused by command, not by reading.
`api/workspaces/[id]/intake/route.ts:87` — `const text = messages.find((m) => m.role === 'user')?.content ?? ''`. `.find()` is the FIRST user turn, and `text` is the ONLY thing the interpret stage sees (line 185). So every later turn re-interprets the opening sentence: the company, the years and the kinds are all frozen at turn 1, and the correction reaches only the stage-2 selection prompt, which can pick from candidates but cannot fetch new ones.
PROVED with a probe over the REAL `maya_issuers` rows, controls included (תיגבור→1460, נכסים→null, "א"→null, all as the module header documents): `resolveIssuer('בז"א')` → **null** (correct — an abbreviation is not a name in the directory), `resolveIssuer('בית זיקוק אשדוד')` → **1361, resolves cleanly**. The clarification the founder typed WOULD have worked. It was never passed to the resolver.
⚠ TWO UNTRUE SENTENCES ON ONE SCREEN, which is the founder's own priority-1 class: the notice said "אין לי מנפיק בבורסה בשם הזה" about a company that IS issuer 1361, and the model — handed local candidates only — said it does not have the files, having never looked.
**THE CLASS, and it is `rules/app.md`'s "A CHOKE POINT IS ONLY AS HONEST AS ITS INPUTS" again, one layer up.** Line 87 was written 2026-08-04 (`5e478c1`, "the intake is a conversation, not a form") when `text` fed only local keyword narrowing — which never executed, the corpus being under the cap — plus the deterministic fallback. For THAT job "the user's opening sentence" was a fair input. On 2026-08-06 MAYA made the interpret stage unconditional and turned it into the company resolver, and it inherited an input chosen for a different question. The route's single exit is still correct; what it decides over is a PROXY — "the first thing they said" standing in for "the company they mean".
NOT YET FIXED, and not a one-liner: naively switching to the latest turn breaks "כן" and "רק את הראשון", which are corrections to a set and not new searches. The interpret stage needs the CONVERSATION, the way stage 2 already gets it. Awaiting the founder — this is the surface tomorrow's agent + chat layer rewrites.
SECONDARY, filed separately because it is real but is NOT what broke this: the directory has no alias/abbreviation layer, so בז"א, בזן, טבע-שם-אנגלי and every other trader shorthand resolve to nothing. `maya_issuers` holds **233 rows with `name_en` NULL on every one of them** — so an English query cannot resolve any issuer today. Coverage is also not "the TASE": the refresh script derives the directory from issuers that SCHEDULED A REPORT in the window it scans.
FOR THE RECORD, the two model questions the founder asked, since neither is written down anywhere: **workspace + intake = OpenAI `gpt-4.1` PRIMARY with `gemini-3.5-flash` hedged in at 6s** (`lib/workspace/askModel.ts`, reversed 2026-08-05 on his instruction); **`/api/chat` = `gemini-3.5-flash` primary with `gpt-4.1` on failure** — the two surfaces lead with DIFFERENT vendors. And there is NO smart layer in either: `git grep` finds no embeddings, no vector store, no tool-calling, no agent loop anywhere in `src`. Retrieval is term-overlap windowing (`lib/workspace/chat/plan.ts`, deliberately built as the seam vectors drop into) and `/api/chat` is context-stuffing at 40k characters (`lib/chat/context.ts`).
[2026-08-10 01:45] LINT — 7 findings (3 fixed inline, 4 filed); worst: **Lane M's state file has NO record of the catalog chapter while the log says it does, and the founder is about to clear that memory.**
Run 5, forced by the counter (5 MERGE lines since the 2026-08-08 LINT, threshold 3). Checks 2, 5, 8, 9 CLEAN.
1 · ⚠ FILED, TIME-CRITICAL — `state-multiview.md` last written **2026-08-09 12:56**, ~7h before the catalog build began (20:10), while cross-cutting `[2026-08-10 00:45]` states *"lane memory is updated to the chapter boundary."* Its `NEXT UP` still points at the calendar week view "once the two branches below are merged" (both merged); its five `catalog` hits are the TASE Data Hub catalogue and the never-created `maya_filings`, none of them this chapter. **A claim in the log contradicted by the artifact it describes — found by `ls`, not by reading.** NOT fixed: the file belongs to a LIVE session and the supervisor does not write another lane's memory. Noted in Lane M's board section; founder told.
2 · ✅ FIXED INLINE — **CRLF graduated to `rules/app.md`.** The only recurring class in the queue with ZERO coverage in any rule: 2 distinct defects, one root cause, one chapter — a scripted multi-line edit that matched nothing against a CRLF tree while the commit message asserted the fix, and both dictionaries committed CRLF with a bare CR welding two keys onto one line (2774 lines of diff for 26 real changes). Bare CR is a legal TS line terminator, so tsc/tests/build all pass. The rule carries the two commands (`git diff --stat` vs `git diff -w --stat`; grep for the intended RESULT) and keeps `* text=auto eol=lf` explicitly OPEN as a founder/fleet decision. Every other repeat class (bidi 36 · timezone 12 · counts 93 · stubs 42 · optimistic-401 26 · choke-point 9) is already in `rules/app.md`.
3 · ✅ FIXED INLINE (this session, pre-lint) — `docs/LAUNCH-KIT.md`'s Lane M prompt and the 🧠 Agent Plan seat both banner-marked; BOARD's MISSION block superseded where it said "⇒ SLICE 4 IS NOT LANE M's TO START", which the founder reversed at 00:20. Check 6's own named failure — *a stale prompt births a lane with a dead mission* — was live at the moment the lint ran.
4 · ✅ FIXED INLINE — brain snapshot taken (check 11). The previous one was **2026-08-08**, i.e. every founder DECISION filed on 08-09 and 08-10 — including tonight's smart-layer decision and Lane M's 00:45 brief — existed in exactly ONE git-ignored copy on one disk.
5 · FILED — **log compaction is due and needs the founder** (check 10): `ready-queue.md` **1764** lines and `cross-cutting.md` **966**, against a 400-line threshold — 4.4× and 2.4× over. Era-compaction moves the old tail verbatim to `docs/archive/`, keeps a distilled summary in the head-era, and only ever with founder approval and only after a snapshot (now taken). `PROGRESS.md` 806 < 1000 = CLEAN.
6 · FILED — `CLAUDE.md` is **428 words** against its ~380 budget (+13%). Deliberately NOT trimmed inline: it is standing law for every session in the fleet, and silently shortening it at 01:45 is how a constraint disappears without anyone deciding to remove it.
7 · FILED — six local branches are merged and unpruned (`feat/company-profiles`, `feat/workspace-tables`, `fix/israel-time-residue`, plus three older). `feat/documents-catalog` is deliberately KEPT: the `Atlas-multiview` worktree still has it checked out and that session is LIVE.
ALSO NOTED, no action: Lane I's section is a month cold (parked, correct) and Lane S never opened (HELD, correct) — both accurately labelled. `docs/superpowers/2026-06-09-v2-results.md` already carries the SHIPPED banner; my first pass flagged it only because the check scanned the subdirectories. Ports are restated in the four LAUNCH-KIT prompts rather than referenced, which check 6 flags — accepted by design, since a pasted prompt cannot dereference a rule, and all four AGREE with `parallel-work.md`.
⇒ **META-REVIEW IS NOW DUE** (~10 merges / one feature cycle): the last cold external audit of the supervisor's own decisions was 2026-07-02. Chapter 3 has since merged six branches. The supervisor must not be the only grader of itself.

[2026-08-10 11:40] SHARED supervisor — .claude/rules/app.md SPLIT (ea83ac7): law stays always-on
(302→183 lines), the 24 verbatim case histories moved to docs/case-history/app.md, linked by
anchor. Always-on preamble 6,508→4,540 words. Every law survived (68/68 by token grep). Bites
you only if you cited app.md by position; cite the anchor. NEW CASES: law here, story there.
Also: ARCHITECTURE.md's "API routes are gated per-route and inconsistently, see the 🔴 entry"
was false since 2026-08-03 and pointed at an entry that never existed — apiAuthBoundary.test.ts
enforces every route (7 allowlisted, each with a reason).

[2026-08-10 12:13] CLOSED supervisor — the ">2MB Pinge snip stripped silently" FINDING (2026-07-23) is FIXED,
not open. `PdfViewer.tsx:232` refuses the capture and raises `onSnipError('toolarge')`; a localized
toast renders at 6 call sites across 4 components; `attachments.test.ts` asserts the exact boundary.
Found while auditing rules/app.md — it had sat on the open list after being fixed. Now removed from
that list. Lesson: a finding closed by a lane that never touched the queue stays "open" forever.

[2026-08-10 12:58] META supervisor — cold meta-review ran (first since 2026-07-07, at 16 merges vs a ~10-merge
law). 20 findings, 3 CRITICAL; of the prior 26: 22 hold, 2 partial, 3 REGRESSED, 1 never done.
Report: docs/audits/2026-08-10-supervisor-meta-review.md. /ship step 6 now carries a META counter.
