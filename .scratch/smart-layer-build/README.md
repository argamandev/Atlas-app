# Smart-layer build — execution tickets

The build sequence of `docs/SMART-LAYER-SPEC.md` §6, one ticket per shippable slice.
Approved by the founder 2026-08-13 (ticket 10 of the retired wayfinder map). Work one
slice per session, branch per slice, ship via `/ship`; a slice is done when its
acceptance line in the spec holds and its budget arithmetic is stated. `Blocked by:`
edges mirror the spec's phases — Phase B and Phase C run in parallel after ticket 04.
