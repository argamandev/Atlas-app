# C3 · Agent chat + memory + reflection ticker

Status: ready-for-agent
Blocked by: 11

Spec §2.8 + §6 C3. Two-fidelity chat (side panel ↔ full page, append-only turns, fresh
session per conversation, continuity from Supabase); visible/editable memory on the
agent profile; `agent_run_events` → visible thinking + the Agents-page reflection
ticker. Acceptance: `/verify-app` both fidelities; memory edit round-trips; ticker live
during a run. Cost: ≤ $0.06/answer.
